//VARIABLES CON LET

let producto = 'tablet'
/*Un dato que yo considero muy importante,
sería que no se le pude poner el mismo nombre
de variable a dos archivos javascript que
estén linkeados al mismo archivo index.html 
porque si no nos suelta error.

NO PODEMOS TENER VARIABLES QUE SE LLAMEN IGUAL
EN EL MISMO NIVEL
*/

/*Con let igual podemos hacer todo lo que podemos
hacer con var.
Reasignar los valores de las variables,
poner números
booleanos
*/
producto = true;

producto = 20; 

producto = null;
console.log(producto);
/*NOTA muy pero que muy importante, que no se nos olvide
El console.log lo que va a hacer es pintar por así
decirlo lo que se encuentre dentro de las llavesitas,
claro, si le ponemos una variable, nos va a pintar el valor 
de esa variable, si hacemos una operación dentro, nos va a dar
el resultado de la operación, si hacemos una operación entre 
dos variables, nos va a dar el resultado de esa operación.
*/

let precio; 
precio = 500;
console.log(precio);

