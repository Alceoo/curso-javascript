//Crear objetos en javascript

const nombre = 'Monitor 20 pulgadas';
const precio = 300;
const disponible = true;

//Un objeto agrupa todo en una sóla variable

//Object literal, asi se llama éste tipo de objeto
const producto = {
  nombre: 'Monitor 20 pulgadas',
  precio: 300,
  disponible: true,
}

/*Cuando veámos una variable que tenga esta sintaxis de variable, nombre de variable=llaves
se trata de un objeto.

Ya creado el objeto lo que tenemos que hacer es meter las variables dentro de ese objeto
y en lugar de utilizar el signo de igual se utilizan los( : ).

A nombre(la variable) se le conoce cómo propiedad o llave del objeto, luego se le pasa 
el valor que queremos que tenga, en caso de nombre es. 'Monitor 20 pulgadas'

Si le queremos agregar más variables siempre se le tiene que poner una coma al finals

*/
console.log(producto);
/*Aquí ya sólo faltaría mandar el valor por consola*/