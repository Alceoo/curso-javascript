const boolean1 = true; 
const boolean2 = false;

console.log(boolean1 === boolean2);

console.log(boolean1 === true);
//si lo marcamos cómo string está mal , obviamente es diferente

//En este caso no tiene sentido que le pongamos el diferente a, ya que pss si es diferente


